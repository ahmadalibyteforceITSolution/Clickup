{
  "name": "clickup-clone-app",
  "version": "1.0.0",
  "private": true,
  "description": "Full-Featured ClickUp Clone in Vue.js and MongoDB with Email Engine",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm --prefix client run build",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "seed": "node server/src/db/seed.js",
    "start": "node server/src/index.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.9.5",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.10.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
